export class Pokemon {
    name: string;
    url: string;
    desc: string;

    constructor(name: string, url: string, desc: string)
    {
        this.name = name;
        this.url = url;
        this.desc = desc;
    }
}
